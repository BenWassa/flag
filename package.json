{
  "name": "flag-atlas",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "A mobile-first adaptive flag and country-location learning PWA.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verify.mjs && node scripts/verify-map.mjs && node scripts/verify-map-v4-edge.mjs && node scripts/verify-routing.mjs",
    "test": "npm run build && npm run verify"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
