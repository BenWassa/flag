name: Fix foundation outline verifier

on:
  push:
    branches: [issue-57-global-expansion-foundation]

permissions:
  contents: write

jobs:
  fix:
    if: ${{ !contains(github.event.head_commit.message, 'Update outline verifier for shared generator') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node .github/fix-foundation-outline-verifier.mjs
      - run: rm .github/fix-foundation-outline-verifier.mjs .github/workflows/fix-foundation-outline-verifier.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/verify-outline.mjs .github
          git commit -m "Update outline verifier for shared generator"
          git push
