name: Issue 151 verification

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary #151 verification workflow; intentionally manual while the PR diff is reviewed.'
